# Copy this file to .env.local for local testing with `vercel dev`.
# Never commit a real key — .env / .env.local are already gitignored.
# Get a key at https://console.anthropic.com

ANTHROPIC_API_KEY=
